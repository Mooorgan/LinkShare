import { SignuploginDataPipe } from './signuplogin-data.pipe';

describe('SignuploginDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SignuploginDataPipe();
    expect(pipe).toBeTruthy();
  });
});
